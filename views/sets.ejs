<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Lego Collection</title>
    <link href="/css/main.css" rel="stylesheet">
</head>
<body>
    <%- include('partials/navbar', { page: '/lego/sets' }) %>

    <section class="container mx-auto px-4 py-8">
        <h1 class="text-4xl font-bold text-center mb-4">Collection</h1>
        <p class="text-center mb-8">Browse the entire collection. Click the theme to filter by themes such as:</p>
        <div class="flex justify-center gap-4">
            <a href="/lego/sets?theme=Christmas" class="btn btn-sm btn-primary text-white hover:bg-purple-700">Christmas</a>
            <a href="/lego/sets?theme=Harry Potter" class="btn btn-sm btn-primary text-white hover:bg-purple-700">Harry Potter</a>
            <a href="/lego/sets?theme=Kingdoms" class="btn btn-sm btn-primary text-white hover:bg-purple-700">Kingdoms</a>
        </div>
    </section>

    <section class="container mx-auto px-4">
        <div class="overflow-x-auto overflow-y-hidden">
            <table class="table table-zebra w-full bg-white shadow-lg rounded-lg">
                <thead class="bg-gray-200">
                    <tr>
                        <th class="py-3 text-left">Image</th>
                        <th class="py-3 text-left">Name</th>
                        <th class="py-3 text-left">Theme</th>
                        <th class="py-3 text-left">Year</th>
                        <th class="py-3 text-left">Parts</th>
                        <th class="py-3 text-left">Details</th>
                    </tr>
                </thead>
                <tbody>
                    <% sets.forEach(set => { %>
                    <tr class="hover:bg-gray-50">
                        <!-- Image Column -->
                        <td>
                           <div class="avatar">
                            <div class="w-32 h-32 rounded-lg bg-transparent bg-no-repeat bg-cover" >
                                  <img style="height:100px; width: 100px;" src="<%= set.img_url %>" alt="" srcset="">
                                </div>
                           </div>
                        </td>
                        <!-- Name Column -->
                        <td class="py-4 font-semibold text-gray-800"><%= set.name %></td>
                        <!-- Theme Column -->
                        <td class="py-4">
                            <a href="/lego/sets?theme=<%= set.theme %>" class="badge badge-primary">
                                <%= set.theme %>
                            </a>
                        </td>
                        <!-- Year Column -->
                        <td class="py-4 text-gray-600"><%= set.year %></td>
                        <!-- Parts Column -->
                        <td class="py-4 text-gray-600"><%= set.num_parts %></td>
                        <!-- Details Column -->
                        <td class="py-4">
                            <a href="/lego/sets/<%= set.set_num %>" class="btn btn-sm btn-outline btn-secondary">
                                Details
                            </a>
                        </td>
                    </tr>
                    <% }); %>
                </tbody>
            </table>
        </div>
    </section>
</body>
</html>
